include:
  - "app/**/*.rb"
  - "lib/**/*.rb"
  - "config/**/*.rb"
  - ".gem_rbs_collection/**/*.rbs"  # Include the RBS files generated by `rbs collection install`
require:
  - "rails"  # Ensure Rails is loaded for IntelliSense
plugins:
  - runtime  # Enable runtime introspection